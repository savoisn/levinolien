<h2>New signature</h2>

<%= render "form.html", changeset: @changeset,
                        action: signature_admin_path(@conn, :create) %>

<%= link "Back", to: signature_admin_path(@conn, :index) %>
